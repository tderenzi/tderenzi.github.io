</div>
<div class="container">
<section class="section project-desc-section">
  <div class="columns is-vcentered is-8">
    <div class="column">
      <h3 class="is-3 title">{{section.title}}</h3>
      {{ section.content | safe }}
    </div>
    {% if custom_image %}
      <div class="column is-half">
          <img src="/assets/images/{{ data.image | url }}" alt="{{ title }}">
      </div>
    {% endif %}
  </div>
  <div class="columns is-vcentered is-8">
    <div class="column is-half">
          <img src="/assets/images/{{ data.image2 | url }}" alt="{{ title }}">
      </div>
      <div class="column is-half">
          <img src="/assets/images/{{ data.image3 | url }}" alt="{{ title }}">
      </div>
</div>
</section>
</div>
